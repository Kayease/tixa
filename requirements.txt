# Media Processing Service - Python Dependencies
#
# This file is for reading the service code locally. The versions that are
# actually installed onto a server come from templates/service-requirements.txt,
# which `tixa create`, `tixa migrate` and `tixa update` all share. Keep the two
# in step.

-r templates/service-requirements.txt
